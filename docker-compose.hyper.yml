version: '2'

services:
    web:
        build:
          context: .
          dockerfile: .docker/Dockerfile-nginx
        ports:
            - "8080:80"

    php:
        build:
          context: .
          dockerfile: .docker/Dockerfile-app

